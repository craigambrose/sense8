{
  "name": "joinacluster",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@architect/workflows": "^3.2.0",
    "npm": "^6.1.0"
  },
  "scripts": {
    "dev": "cd client; npm run build; cd ..; npx sandbox",
    "start": "nodemon --watch src/ -e md,css,js --exec NODE_ENV=testing arc-sandbox"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
